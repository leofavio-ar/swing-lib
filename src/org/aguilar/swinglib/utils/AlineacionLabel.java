/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package org.aguilar.swinglib.utils;

/**
 *
 * @author Leo Aguilar
 */
public enum AlineacionLabel {
    /** El valor que indica la alineación izquierda, equivale a <code>OrientacionLabel.LEFT</code>. */
    LEADING,
    /** El valor que indica la alineación izquierda. */
    LEFT,
    /** El valor que indica la alineación derecha, equivale a <code>OrientacionLabel.RIGHT</code>. */
    TRAILING,
    /** El valor que indica la alineación derecha. */
    RIGHT,
    /** El valor que indica la alineación centrada. */
    CENTER
}